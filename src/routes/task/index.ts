import { FastifyInstance, FastifyPluginOptions, FastifyError } from 'fastify';

// const getTask









export default function taskRoutes(fastify: FastifyInstance, options: FastifyPluginOptions, done: (err?: FastifyError) => void) {
  // fastify.get('/tasks/:project_id')
  // fastify.post('/task/:id')
  // fastify.delete('/task/:id')
  // fastify.put('/task/:id')
  done()
}